@page "{id:long}"
@model ServiceHost.Areas.Administration.Pages.Accounts.Role.EditModel
@{
}
@*<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h4 class="modal-title">ویرایش گروه محصول</h4>
    </div>*@
<form method="post" enctype="multipart/form-data">
    <div class="modal-body">
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label for="Name" class="control-label">نام</label>
                    <input type="text" class="form-control" name="Name" asp-for="editRole.Name" >
                    
                    <span asp-validation-for="editRole.Name" class="error"></span>
                </div>
               
            </div>
            
        </div>
        
        
    </div>
    <input type="hidden" class="form-control" name="Id" asp-for="editRole.Id" value="@Model.editRole.Id">
    <div class="modal-footer">
        <button type="button" class="btn btn-default waves-effect" data-dismiss="modal">بستن</button>
        <button type="submit" class="btn btn-info waves-effect waves-light">ایجاد</button>
    </div>
</form>